import { Helmet } from "react-helmet-async";

const Products = () => {
    return (
        <div>
          <Helmet>
                <title> TechDiscoverly | Products</title>
              </Helmet>    
        </div>
    );
};

export default Products;